<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#49627a</item>
        <item name="colorPrimaryDark">#233c54</item>
        <item name="colorAccent">#f9dc5c</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!--<item name="android:textColorPrimary">@android:color/primary_text_material_dark</item>-->
        <!--<item name="android:textColorPrimaryInverse">@android:color/primary_text_material_light</item>-->
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Button" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

</resources>
